const breakpoints = {
  sm: 576,
  md: 768,
  lg: 1024,
};

export const sm = `max-width: ${breakpoints.sm}px`;

export const md = `max-width: ${breakpoints.md}px`;

export const lg = `max-width: ${breakpoints.lg}px`;

export default breakpoints;
